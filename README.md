# Мониторинг подписки ПРАЙМ

<!--
  Это шаблон. Заполните разделы ниже своими словами и удалите комментарии
  вроде этого. README — первое, что открывает человек, зашедший в ваш
  репозиторий, и на ДЗ-1 за него стоит 1 балл из 10.

  Критерий простой: сможет ли посторонний человек по этому файлу запустить
  ваш проект, ничего у вас не спрашивая.
-->

Учебный проект курса «Программирование на Python для бизнес-аналитики»
(ВШБ НИУ ВШЭ, БАЦРФ, 2026/2027).

**Автор:** <!-- фамилия, имя, учебная группа -->

---

## Что здесь

<!--
  Два-три предложения о том, что делает проект на сегодня. Не «учебный
  проект по курсу», а по существу: какие данные берёт, что считает,
  что показывает. По мере курса этот раздел будет расти.
-->

---

## Как запустить

Нужны [Git](https://git-scm.com/) и [uv](https://docs.astral.sh/uv/).
Python отдельно ставить не нужно — его поставит uv.

```bash
git clone git@github.com:ВАШ_ЛОГИН/prime-monitor.git
cd prime-monitor
uv sync
```

Доступ к учебной базе выдаёт бот курса по команде `/db`. Полученную строку
подключения кладём в `.env`:

```bash
cp .env.example .env
# и вписать в .env строку из бота
```

Проверка, что всё встало:

```bash
uv run python -c "from prime.config import get_engine; print(get_engine().connect().execute(__import__('sqlalchemy').text('select current_user')).scalar())"
```

Запуск JupyterLab:

```bash
uv run jupyter lab
```

---

## Структура

| Путь | Что это |
|---|---|
| `src/prime/` | код проекта: подключение, метрики |
| `notebooks/` | ноутбуки: разведка, расчёты, графики |
| `pyproject.toml` | какие библиотеки нужны проекту |
| `uv.lock` | какие версии встали на самом деле |
| `.env.example` | образец файла с доступами; настоящий `.env` в репозиторий не идёт |

---

## Что здесь считается

<!--
  Заведите этот раздел, когда появится первая метрика. Для каждой:
  как она определена и на каких данных считается. Через месяц вы сами
  не вспомните, включали ли вы в выручку возвраты.
-->

---

## Использование ИИ

См. [ДЕКЛАРАЦИЯ-ИИ.md](ДЕКЛАРАЦИЯ-ИИ.md).
